{
  "name": "@promptgenius/core",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Framework-free engine: platform adapters, token estimation, cost, scoring. No UI, no network.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit && tsc --noEmit -p scripts",
    "calibrate": "tsx scripts/calibrate.ts"
  },
  "dependencies": {
    "gpt-tokenizer": "^4.0.0",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "tsx": "^4.23.15"
  }
}
