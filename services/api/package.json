{
  "name": "@promptgenius/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "API handlers (Hono). Runs on Node locally; the same app deploys to AWS Lambda in Phase 3.",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.128.0",
    "@google/genai": "^2.24.0",
    "@hono/node-server": "^2.1.1",
    "@promptgenius/api-contract": "workspace:*",
    "@promptgenius/core": "workspace:*",
    "hono": "^4.13.8",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "@types/node": "^26.6.2",
    "tsx": "^4.23.15"
  }
}
