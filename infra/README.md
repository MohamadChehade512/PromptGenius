# infra

AWS CDK (TypeScript) app, added in **Phase 3 (M10)**. See PLAN.md §3.3 for the target architecture:
S3 + CloudFront + WAF, API Gateway + Lambda, a Lambda Function URL for streaming rewrites,
Secrets Manager, DynamoDB, and GitHub OIDC deploys.

Intentionally empty until then. The app runs fully locally in Phases 1–2.
